display.plan:
  version: 1.x
  css:
    theme:
      css/display-plan.css: {}
  # Disabling for now, it really breaks stuff without it being smart
  # enough to only make parents and siblings sticky.
  # js:
    # js/jquery.stickybits.3.6.4.min.js: { minified: true }
    # js/display-plan.js: {}
  dependencies:
    - core/jquery
